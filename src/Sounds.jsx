define(function(require) {
  /*const $planeSound = document.getElementById("plane_sound");
  const $musicSound = document.getElementById("music_sound");

  function playTilItsGood($sound) {
    $sound.play().catch(e => setTimeout(() => playTilItsGood($sound), 500));
  }

  function volumeDown() {
    $planeSound.volume = $planeSound.volume - 0.0002;
    if ($planeSound.volume >= 0.1) requestAnimationFrame(volumeDown);
  }

  function volumeUp() {
    $planeSound.volume = $planeSound.volume + 0.0005;
    if ($planeSound.volume <= 0.3) requestAnimationFrame(volumeUp);
    else setTimeout(volumeDown, 4000);
  }

  function init() {
    $planeSound.volume = 0;
    playTilItsGood($planeSound);

    setTimeout(volumeUp, 4000);
  }

  init();

  playTilItsGood($musicSound);*/
});
